---
description: Use when a project wants a structured adversarial review record before `gh pr create` or `gh pr merge`. Outputs categorized findings (Block / Must Fix / Suggestion / Nit) for the review lane selected by the current session and Host policy.
---

# /review-pr — Structured PR Review

You are performing the review lane selected by the current session: author
self-inspection, optional triage, or independent final review. The session/Host
policy chooses the model, profile, isolation, and dispatch mechanism. The
author's self-inspection prepares the handoff; it does not replace an
independent final review when policy or risk requires one.

## Step 1: Identify scope

```bash
BASE_REF="${ARGUMENTS:-${BASE_REF:-main}}"
git diff "$BASE_REF"...HEAD --stat
git log "$BASE_REF"..HEAD --oneline
```

If a branch arg was passed (`$ARGUMENTS`), use it as the base reference. Otherwise
set `BASE_REF` to the base recorded in the T-file; `main` is this command's
fallback example.

## Step 2: Read full diff

```bash
git diff "$BASE_REF"...HEAD
```

Read the entire diff. Do not skim. Every changed file is in scope.

## Step 3: Adopt adversarial persona

You are a **skeptical senior engineer**. Your job is NOT to validate — find bugs, security holes, performance issues, convention violations. **Assume the code is WRONG until proven otherwise.** Be specific: cite file paths and line numbers. Do not say "looks good" without concrete technical justification. Do not praise.

Use the adversarial perspective regardless of model. Watch for shortcuts such as:
- Silent error swallowing (catch blocks with `console.log` and no rethrow)
- Mocking internals instead of boundaries → mocked-vs-real shape drift
- Skipping edge cases under time pressure (empty arrays, null inputs, max values)
- Hasty SDK choices without checking docs
- Missing cross-tenant safety (no `tenant_id` filter, no defensive assertion)
- RLS bypass via service_role where anon should suffice
- Speculative abstractions added "for the future" (YAGNI violations)

## Step 4: Execute checklist

For each file changed in the diff:

### Correctness
- Logic matches what the T-file or PR description claims?
- Error paths handled (no swallowed exceptions, no silent fallbacks that hide failures)?
- Edge cases covered (empty inputs, null, max values, race conditions, concurrent writes)?
- Async flows awaited correctly (no floating promises)?

### Security (OWASP top 10 relevant subset)
- Input validation at all trust boundaries?
- AuthZ/AuthN checks present where needed (not just AuthN)?
- SQL injection prevented (parameterized queries, ORM, no string concat)?
- XSS prevented (no raw HTML injection with user-controlled data)?
- SSRF prevented (no user-controlled URLs passed to server-side fetch without allowlist)?
- Secrets not hardcoded in source?
- Cross-tenant isolation preserved (RLS enabled, `tenant_id` in every domain query, defensive assertion after fetch)?

### Conventions (project-specific)
- Conventional Commits format in all commits? No `Co-Authored-By: Claude` trailer?
- TDD evidence: RED/GREEN commands, trees, exit codes, and logs reach the intended behavior? If RED was reproduced later, is it labeled as a reproduction rather than historical evidence?
- Files outside declared scope of T-file not touched (YAGNI / scope creep)?
- No speculative abstractions or future-proofing not in spec?
- Comments explain WHY (non-obvious intent), not WHAT (code already shows what)?
- Imports clean (no unused imports, no circular deps)?
- Base/candidate and pending changes match the handoff?

### Test coverage (regression policy)
- If `fix:` commit — regression test that fails before fix and passes after?
- If `feat:` commit — the applicable test layer covers the happy path and the selected error path?
- If a new service boundary exists — applicable contract/integration evidence is present?
- Mock boundaries correct (mock external HTTP, not internal functions)?
- Are skipped or pending checks explained rather than silently treated as green?

Do not run the entire suite by reflex. Run the focused checks required by the
task impact and project policy, and record commands, exit codes, and logs.

## Step 5: Categorize findings

Output the following markdown block:

```md
## Self-review findings

### Block (do not integrate until resolved)
- `path/to/file.ts:42` — [description of issue] — [suggested fix]

### Must Fix (fix before merge; can open PR draft with TODO)
- `path/to/file.ts:87` — [description] — [suggested fix]

### Suggestion (optional improvement, can defer)
- `path/to/file.ts:23` — [improvement] — [reasoning]

### Nit (style/clarity, low priority)
- `path/to/file.ts:11` — [nit] — [reasoning]

### Verified (checked and found OK — cite what you checked)
- Correctness of [X]: [brief reasoning]
- Security of [Y]: [brief reasoning]
- Test coverage of [Z]: [brief reasoning]

### Evidence and review lane
- Base/candidate/worktree: [references]
- Commands, trees, exit codes, logs: [references]
- Review lane: [self-inspection | triage | independent final review]
- Triage status: [not applicable | confirmed | inconclusive | out-of-scope]
```

If a category has no findings, write `<!-- None -->` under its header.

## Step 6: Report and route

### Block findings present
Do not integrate the candidate. Either:
- Fix the issue yourself (if within scope)
- Open a targeted message/task to the specific worker that generated the file

The author reproduces and corrects confirmed findings. Re-run `/review-pr` or
the selected review lane after the correction, and check the changed delta.

### Only Must Fix / Suggestion / Nit
Include findings in PR body under `## Orchestrator self-review findings`.

```bash
gh pr create --title "..." --body "$(cat <<'EOF'
## Summary
...

## Orchestrator self-review findings
[paste findings markdown here]

## Test plan
...
EOF
)"
```

### All Verified (no Block/Must Fix)
Note in PR body: `Review pass: no blocking findings. See verified items above.`

### Optional triage pass
If this command is being used for triage, it reports findings and does not
approve the candidate. Use the evidence contract in `xp-stack:akita-xp-rules`;
record location, concrete case, expected versus observed, evidence, severity,
and confirmation. The author corrects confirmed findings, the recheck covers
the changed delta, and an independent final reviewer evaluates the candidate.
Unknown or inconclusive evidence is not approval.

## Step 7: Authorized integration

After the review pass, integrate or merge according to the current session and
project authorization. Do not infer a fixed actor or impose a human-only merge
rule when the authorized flow permits automation. Required independent review
and security gates still apply.
