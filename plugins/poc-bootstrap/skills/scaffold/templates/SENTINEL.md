# POC Sentinel

POC_SENTINEL_FILE

Arquivo marcador usado por `poc-bootstrap` pra validar que `scaffold.sh`
copia arquivos de `templates/` pro cwd do projeto receptor. Nao tem papel
funcional alem de ser grep-avel pela string `POC_SENTINEL_FILE`.
